import { getForm } from '$server/dbForm'

export async function load({ locals }) {
	// return {
	// 	auth_account: await getForm('form_auth_account', locals.user),
	// 	auth_verify_phone_mobile: await getForm('form_auth_verify_phone_mobile')
	// }
}
