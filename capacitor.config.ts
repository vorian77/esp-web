import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kssc.espmobile',
  appName: 'esp-mobile',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
